# This file defines KITTI paths, so required data can be found
# It is assumed that KITTI dataset follows certain order, but
# you can have different order if you configure these paths.

data_root = "/home/joni/data/Datasets/KITTI/"
train_anno = data_root + "/kitti_infos_train.pkl"
val_anno = data_root + "/kitti_infos_val.pkl"
test_anno = data_root + "/kitti_infos_test.pkl"
db_info_path = data_root + "/dbinfos_train.pkl"
